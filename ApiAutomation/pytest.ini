[pytest]
# 默认运行保持离线且不生成仓库文件；需要报告时显式传入 --html。
addopts = -ra
# 测试搜索路径
testpaths = ./tests
# 测试文件命名规则
python_files = test_*.py
# 测试类/函数命名规则
python_classes = Test*
python_functions = test_*
markers =
    api: tests that call real external APIs
